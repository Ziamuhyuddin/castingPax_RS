const imagePath = '../assets/images';
const iconPath = '../assets/icons';
const extension = '.png';

const Appimages = {
  // LOGO: require(`${imagePath}/qrcode${extension}`),
};

const Appicons = {
  DisVis: require(`${iconPath}/ic_DisVis${extension}`),
  EnaVis: require(`${iconPath}/ic_EnaVis${extension}`),
};

export {Appimages, Appicons};
